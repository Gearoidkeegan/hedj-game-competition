{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json; d=json.load(sys.stdin); f=d.get('tool_response',{}).get('filePath') or d.get('tool_input',{}).get('file_path',''); f.endswith('.html') and __import__('subprocess').run(['npx','htmlhint',f])\" 2>/dev/null || true",
            "statusMessage": "Linting HTML..."
          }
        ]
      }
    ]
  }
}
